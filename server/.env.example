# Server Configuration
PORT=3001

# CORS Configuration
# Use '*' to allow all origins, or specify a specific origin like 'http://localhost:3000'
CORS_ORIGIN=*

# Database Configuration
DB_HOST=localhost
DB_PORT=54321
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=stock_app

# JWT Configuration (if needed)
# JWT_SECRET=your-secret-key-here
